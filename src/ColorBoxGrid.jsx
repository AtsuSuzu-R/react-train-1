import ColorBox from "./ColorBox";

export default function ({ colors }) {
  return (
    <>
      <ColorBox colors={colors} />
      <ColorBox colors={colors} />
      <ColorBox colors={colors} />
      <ColorBox colors={colors} />
      <ColorBox colors={colors} />
    </>
  );
}
